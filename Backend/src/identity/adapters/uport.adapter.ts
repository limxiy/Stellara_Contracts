export class UPortAdapter {
  async importCredential(uportPayload: any) {
    // TODO: parse uPort credentials
    return uportPayload;
  }

  async exportCredential(vc: any) {
    // TODO: format for uPort
    return vc;
  }
}
